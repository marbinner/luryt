---
name: improve-luryt
description: Stress-test one narrowly scoped part of the Luryt constructed language and propose or implement exactly one evidence-backed change per run. Use when asked to improve, evolve, extend, challenge, or stress-test Luryt grammar, morphology, phonology, particles, roots, semantics, spatial constructions, questions, or numbers. Not for tooling-only bugs, ordinary code changes, or wording edits that do not alter the language.
---

# Improve Luryt (pointer)

The canonical skill lives at `.agents/skills/improve-luryt/SKILL.md` so every agent toolchain
shares one copy. Read that file in full and follow it exactly, including both references
(`references/stress-test-playbook.md` and `references/audit-integrity.md`) and the sealed
audit gates driven by `scripts/check_audit.py` — all paths relative to the canonical skill
directory. Do not duplicate its content here; if the two copies ever disagree, the `.agents`
copy wins.
